<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sexy Naughty Hub</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: Arial, sans-serif;
            background-color: #111; /* Dark background for that naughty vibe */
            color: #fff;
        }
        #title {
            text-align: center;
            padding: 20px;
            font-size: 2.5em;
            color: #ff4444; /* Hot red color */
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }
        #content-frame {
            width: 100%;
            height: calc(100vh - 80px); /* Fills the rest of the screen */
            border: none; /* No ugly iframe border */
        }
    </style>
</head>
<body>
    <h1 id="title">Get Ready to Feel the Heat, Baby! 😈</h1>
    <iframe id="content-frame" src="https://xnxx.health/search/xnxx" allowfullscreen></iframe>

    <script>
        // Optional: Rotate sexy titles for extra fun
        const sexyTitles = [
            "Get Ready to Feel the Heat, Baby! 😈",
            "Naughty Nights Await You, Jaan! 🔥",
            "Let’s Get Wild and Dirty Tonight! 😝",
            "Your Desi Desires Come True Here! 💋"
        ];
        let currentIndex = 0;
        const titleElement = document.getElementById('title');

        function changeTitle() {
            titleElement.textContent = sexyTitles[currentIndex];
            currentIndex = (currentIndex + 1) % sexyTitles.length;
        }

        // Change title every 5 seconds
        setInterval(changeTitle, 5000);
    </script>
</body>
</html>
